根据题意，把奇数编号的节点放到左边，把偶数编号的节点放到右边，那么这样的话，使用递归，递归三要素：返回值 调用单元 终止条件
我们需要构建一个奇数编号链表和一个偶数编号链表，维护奇数节点链表的尾节点和偶数节点链表的尾节点，遍历链表，根据当前的节点的编号选择是加到奇数链表尾部还是偶数链表尾部，随后把其加到末尾，更新尾部节点指针，同时注意每次更新尾部节点指针时，需要把指针->next设置成nullptr，这样可以避免链表交叉连接的错误。
最后需要维护一个偶数节点的头指针，把奇数链表的尾节点和这个头指针相连即可，返回head不变。
