贪心法，每个阵营的参议员优先干掉对方离得最近的参议员，使得己方利益最大化。
可以设两个队列，队列中存储的是当前阵营参议员的位置信息，每次比较的时候，从两个队列中弹出两个元素，下标小的优先干掉对方下标较大的，同时因为他已经投过票了，需要重新入队，这里需要注意，同一个参议员在一轮中只能发挥一次作用，他只能被该轮中靠后的对方选手干掉或者在下一轮中发挥作用，所以要设置一个位置偏移量，保证逻辑正确性，在这里同时给这样的元素加上len，保证了参议员的相对位置正确。
