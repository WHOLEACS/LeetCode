求有序数组的下一个排列，首先需要找到当前数组中的最后一个高点，如果最后一个高点在开头，那么说明数组是[3,2,1]这样的状态，此时按照题意把数组翻转即可。
如果最后一个高点在末尾，那么说明数组是[1,2,3]这样的状态，此时直接交换末尾两个元素即可。
从特殊到一般，如果最后一个高点不在上述位置，那么要找到下一个更大的排列，那么必须在高点前一个位置填充一个比当前值更大的数，不一定是高点本身，需要遍历高点及以后位置上的元素，找到刚好大于高点前一个位置上的元素，得到后进行交换，观察此时的状态，从高点到数组末尾此时是一个降序排列的有序数组，此时高点前一个位置上已经填充了一个更大的数，这个排列比原来大，但是同时我们需要从高点到最后的这一个部分尽可能的小，这样才不会漏掉中间的情况，所以我们需要对这一个部分的数组进行交换处理。
