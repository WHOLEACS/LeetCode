本题要求的是山脉数组，山脉数组的条件就是先升后降且相邻元素不能相等，这样的话我们可以首先用一个bool值表示当前状态是升序还是降序，并且当第一个A[i] > A[i+1] 的时候把flag设置成降序，这样的话如果当前相邻元素的大小关系不满足升降序的时候，可以直接return false；
需要注意的是，如果一个序列之后升序，这个时候循环会正常结束，但是不满足山脉数组，此时应该对flag进一步判断即可。
时间复杂度：O(n) 空间复杂度：O(1)
