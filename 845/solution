很容易想到动态规划的做法，创建两个数组，对于每个元素来说，left数组元素表示左边比当前小的连续元素的个数，right数组元素表示右边比当前小的连续元素的个数，这样的话，通过填充left数组和right数组，我们就可以知道所有的元素的左右两边的情况。时间复杂度：O(n) 空间复杂度：O(n)
时间复杂度不可以继续实现更大的优化，但是空间复杂度可以通过双指针实现O(1)的优化
使用双指针，每次左边的指针l指向当前开始位置，r指针右移，寻找一个先上升后下降的可能的最大区间，维护两个bool值表示当前区间是否发生过上升和下降。
通过左右指针迭代进行的方式，实现了空间复杂度的优化。
