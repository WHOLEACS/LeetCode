这道题主要使用了双指针的做法，主要需要应对的是字符重复的问题。
首先设置两个指针分别指向name串和type串的开头，进入while循环，我们首先需要知道name串的指针指向的当前字符连续重复出现几次，得到结果后，name串的指针目前已经指向了下一个不重复的字符；对于type串来说，需要第一步判断它目前指向的字符和name串目前重复的字符是否相同，如果不同直接返回false，否则需要进行type串的遍历，当次数不足以达到该字符在name串中出现的次数时，返回false。在结束一次循环后，两个指针分别指向了下一个需要判断的位置。
在退出循环后，当两个指针的位置都位于串的末尾时，返回true；否则返回false。
